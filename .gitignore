# Build artifacts
bin/
*.exe
*.test
pgrouter-linux
pgrouter-pgo-*
pgrouter-phasea-b
{{.Destination}}

# Bench artifacts
heap.pprof

# Coverage
coverage.out
coverage.html

# IDE
.idea/
.vscode/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Local configs (real ones; samples in configs/ are tracked)
*.local.yaml
pgrouter.yaml
!configs/pgrouter.yaml
!test/bench/compare/pgrouter/pgrouter.yaml
!test/bench/compare-phasea/pgrouter/pgrouter.yaml

# Secrets
*.key
*.pem
!test/tls/*.key
!test/tls/*.pem

# Security incident post-mortem (contains VPS creds, attacker IPs, exploit details)
VPS_INCIDENT_POSTMORTEM.md

# Internal design notes (not tracked in git)
DESIGN_NOTES.md

# Local VPS transfer tars (VPS payloads used for one-off file sync)
splice-payload*.tar
splice-payload*.tar.b64
